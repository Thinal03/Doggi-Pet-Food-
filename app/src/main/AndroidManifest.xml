<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doggi"
        tools:targetApi="31">

        <!-- Other Activities -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Buyer.BuyerHomePageActivity" />
        <activity android:name=".Buyer.ProductActivity" />
        <activity android:name=".Buyer.EduContentActivity" />
        <activity android:name=".Buyer.PersonalInformationActivity" />

        <activity android:name=".Seller.SellerHomePageActivity" />
        <activity android:name=".Seller.SellerPersonalInformationActivity" />
        <activity android:name=".Seller.SellerEduContentActivity" />
        <activity android:name=".Seller.SellerProductsActivity" />
        <activity android:name=".Seller.AddProductActivity"/>



        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Doggi">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>